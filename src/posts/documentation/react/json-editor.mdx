import { MDXCodeSample } from '../../../app/(docs)/documentation/[framework]/[slug]/code-sample.tsx'

# JSON editor

The JSON web editor is a React component designed to show editable JSON data within a web editor.

<MDXCodeSample theme="amy" height="85px" readonly={false} />
<MDXCodeSample theme="barf" height="85px" readonly={false} />
<MDXCodeSample theme="bespin" height="85px" readonly={false} />
<MDXCodeSample theme="cobalt" height="85px" readonly={false} />
<MDXCodeSample theme="coolGlow" height="85px" readonly={false} />
<MDXCodeSample theme="clouds" height="85px" readonly={false} />

## How to use the JSON Editor ?

Just import the component:

``` import { JsonEditor } from 'webeditors-react' ```

And use it in your JSX/TSX:

``` example ```